# Mentor briefing deck source

Build (needs node + pptxgenjs):

```
cd proto/deck_src && npm install && node build_deck.js
```

Writes `../out/Orb_v1_Mentor_Briefing.pptx`. Figures come from `../out/fig_*.png`.
