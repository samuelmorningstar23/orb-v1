{
 "workstream": 6,
 "task": "C4 task 2 acceptance pass on the post-freeze data: sealed block / landing scorecards on the 21:37 aggregate and 21:53 scorecards, Ghost audit hidden-stop + regret, Scenario Explorer across both pre-race fidelities, red-team live_position_claim, reversible driver-feedback posterior, Race Twin offline at 1440x900 and 1920x1080, goldens only for routes the new data changed",
 "status": "GREEN",
 "commit": null,
 "completed": [
  "AC1 PASS: sealed block renders the post-freeze aggregate (quotable true, freeze non-null, reveal record) labelled 'sealed holdout, aggregate only'. Generalisation prints 'MAE Orb v1 vs naive 0.0372 [0.0252, 0.0457] vs 0.1713 [0.0952, 0.2252] s/lap', 90% band coverage 100%, 6 sealed / 5 forecastable / 1 weekend-level abstention, compound-weekends 9 (5 issued, 4 withheld); landing prints the same MAE pair, coverage and counts. pages/generalisation.py + pages/landing.py now print the sealed MAE at 4 dp (SEALED_DP) so the dashboard equals the lead's quotable headline instead of rounding it to 0.037/0.171. Verified equal to out/validation/holdout_aggregate.json and to ghost_scorecard.json.sealed_holdout (identical dicts); no sealed race id and no per-race value anywhere on the page (holdout_per_race.json never opened: existence and 56683 bytes only).",
  "AC1 gate PASS: pointed at a dry-run aggregate (quotable false) the same pages show 'sealed: 6 weekends, aggregate revealed after freeze' and none of its numbers (new test test_sealed_block_stays_gated_for_a_dry_run_aggregate).",
  "AC2 PASS: Ghost audit rail renders Workstream 3's rows verbatim for the selected weekend - Monza/VER '6 stops scored - VER in-lap L28 medium -> new hard (sc/vsc): next-lap error 0.6 s vs naive 1.1 s - inside the 90% interval - 3-lap 2.0 vs 3.4 - 5-lap 3.8 vs 5.9', 2026 pool 'next-lap MAE 1.0 vs naive 1.4 ... 210 stops, 10 weekends', regret 'Orb v1 S-M (44/9) +0.0 s - naive M-H +47.1 s - observed field +66.3 s - default +42.8 s - hindsight oracle M-S (10/43) - pit loss 21 s' under the exact label 'held-out strategy replay under a post-race reference model' plus 'development pool (leave-one-weekend-out), never a sealed weekend'. Values match hidden_stop_2026.json / regret_2026.json (both unchanged since 16:49). Monza/NOR has no scorable stop and says so with the weekend's skip reasons.",
  "AC3 FIXED: the explorer ignored the fidelity control and always showed fixed_context. pages/ghost_strategy.py now resolves the pre-race scenario at the selected fidelity (falling back to the built one and saying so on a new 'simulation fidelity' row), and defaults the control in scenario mode to the fidelity Workstream 2 actually built (fixed context first), so the default view still shows -14.2 s. fid=fixed_context -> -14.2 s / q10-q90 -60.7 to +40.9 / p 62%; fid=tyre_only -> -16.6 s / -65.9 to +41.5 / p 63%, each equal to its own summary.json (-14.21868917519702 / -16.647211706384688) and each labelled 'model-implied, pre-race curve'; the other fidelity never leaks into the rail.",
  "AC4 PASS: no projected rejoin P-number on any live render (live_predictor and decision_board at Monza/NOR lap 30 and 44, Barcelona/PIA lap 35, Hungary/NOR lap 30): zero '~P', 'projected rejoin', 'P<n> ->' hits; every one carries 'observed gap structure, not a position forecast'. The live_position_claim regex has exactly one match in app_v2 (services/live_bridge.py:99, the observed position_now), now carrying the lead's frozen_field context comment, so evaluation.red_team.claim_audit.audit_wording returns allowed_with_context for it - and zero 'reword' verdicts for every rule over every source.",
  "AC5 PASS (new test test_driver_feedback_shifts_posterior_and_is_reversible): at Monza/NOR lap 24 a rear-traction overheating severity-5 event at lap 22 moves the rendered 7.1 posterior from 'degradation_rate +0.015 s/lap' to '+0.014 s/lap' (confidence_effect sentence: slope posterior +0.0151 +- 0.0357 -> +0.0148 ...), and removing the event restores +0.015 exactly. All three log paths (app_v2.services.paths.FEEDBACK_LOG, live.session.FEEDBACK_LOG, live.viewmodel.FEEDBACK_LOG) are redirected to tmp_path and the test asserts app_v2/state/feedback_events.jsonl is byte-identical at the end (still 0 bytes, mtime 21:33).",
  "AC6 PASS: Race Twin on the Ghost route at 1440x900 and 1920x1080 - 0 console errors, 0 external requests attempted, no horizontal overflow (scrollWidth == innerWidth), cold load 872-1010 ms, player iframe present, 4926 frames for Monza/NOR from out/maps/Monza (assets_status ok, 575 track points). Scripted pass re-run 22:24: replay to Lap 53/53, ghost switch 278 ms with the player iframe, 0 console errors, 0 external requests.",
  "AC7 PASS: capture.py --compare run twice before any rewrite (identical to 3 dp, so the diffs are content not noise), then --update --routes for the 10 routes with a content cause (scenario_explorer 0.665/1.546%, prerace 1.454/1.459%, landing 0.000/0.267%, validation 0.000/0.179%, offline_mode 0.137/0.087%, presentation_ghost 0.015/0.184%, out_of_support 0.000/0.025%, ghost_audit 0.016/0.002%, ghost_audit_fixed_context 0.016/0.001%, generalisation 0.008/0.005%). The 9 unchanged routes (live_stable, live_after_feedback, decision_change, decision_board, presentation_live, feedback, missing_position, degraded_feed, position_refused) were left untouched and are byte-identical. 19 of 38 PNGs changed bytes; after the rewrite every route compares at 0.000% except the ghost-audit canvas jitter (0.002-0.006%).",
  "Extra finding fixed in the UI: out/maps/Monza frames for monza_ver_lap20_to_hard_new_fixed_context predate the 21:39 scenario regeneration (frames finish delta -6.5 s vs scenario +7.7 s, the 13.5 s identity-check failure the lead logged). pages/ghost_strategy.frames_behind_scenario now prints 'PLAYER FRAMES PREDATE THIS SCENARIO' with both numbers on that combination and stays silent on the three consistent ones, so the animation is never read as the current scenario (new test). Not a golden route; no golden churn.",
  "PERF.md: C4 acceptance section with the per-route diff table (before, cause, rewritten, after), the diff_ratio caveat (pixels differing by more than 24 levels, so 0.000% is not byte equality) and the new behaviour."
 ],
 "in_progress": [],
 "blockers": [],
 "tests_passed": 67,
 "tests_failed": 0,
 "eta_minutes": 0,
 "needs_review_from": [
  4,
  8,
  "lead"
 ],
 "notes": "Command: ../.venv/bin/python -m pytest tests/ui tests/screenshots -q -p no:cacheprovider -> 67 passed in 101 s (tests/ui 56, 5 added; tests/screenshots 11). The 8502 dashboard was restarted twice (22:09 and 22:20, now pid 91227, same command line) because Streamlit does not reload edited modules; it serves the current code. LEAD: at 22:01 decision/optimizer.py was briefly unparseable - the red-team comment on line 117 had been placed inside the multi-line dict, so `import live` failed and every live route would have fallen back to PLACEHOLDER; it was fixed at 22:01 before I re-ran anything, and live/viewmodel.py:174-175 now carries the same context. No action needed, flagged only so the C4 commit is known to include the fixed version (ast.parse OK at 22:02). WORKSTREAM 4: please re-run `python -m replay.build_maps` for Monza - frames_VER_monza_ver_lap20_to_hard_new_fixed_context.npz is from 16:31 and encodes the superseded scenario (-6.5 s vs the regenerated +7.7 s); the page now warns instead of animating it silently. The other three Monza frame sets match their scenarios to within 0.25 s. WORKSTREAM 8: live/session.py still hard-codes FEEDBACK_LOG, so honouring ORB_FEEDBACK_LOG there would let the feedback test and a screenshot server use a fixture log without monkeypatching three modules. WORKSTREAM 3: out/validation/*.json still have no .sha256 sidecars (shown as 'unsigned' with the computed hash); ghost_scorecard.json 21:53 now carries sealed quotable true and matches holdout_aggregate.json exactly. WORKSTREAM 2: both pre-race fidelities are now wired to the explorer's control; a pre-race scenario for a second driver would let the control be demonstrated on VER too. Shared state untouched: app_v2/state/feedback_events.jsonl is still 0 bytes at mtime 21:33; no file outside app_v2/, tests/ui/, tests/screenshots/ and this heartbeat was written.",
 "updated_at": "2026-09-12T22:30:35"
}
